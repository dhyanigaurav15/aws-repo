- name: Ec2 get facts
  ec2_instance_info:
    region: "us-east-1"
    filters:
      instance-state-name: [ "running" ]
  register: ec2

- set-fact: ec2id="{{ ec2.stdout_lines }}"
  
- name: Print name
  debug:
      var: ec2id
